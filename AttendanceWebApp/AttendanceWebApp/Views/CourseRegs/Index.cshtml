@model IEnumerable<AttendanceWebApp.Models.CourseReg>

@{

    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.course_code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.student_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.course_code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.student_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.course_regID }) |
            @Html.ActionLink("Details", "Details", new { id=item.course_regID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.course_regID })
        </td>
    </tr>
}

</table>
